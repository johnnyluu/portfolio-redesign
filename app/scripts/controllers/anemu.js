'use strict';

/**
 * @ngdoc function
 * @name repoApp.controller:AnemuCtrl
 * @description
 * # AnemuCtrl
 * Controller of the repoApp
 */
angular.module('repoApp')
  .controller('AnemuCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
