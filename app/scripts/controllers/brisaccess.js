'use strict';

/**
 * @ngdoc function
 * @name repoApp.controller:BrisaccessCtrl
 * @description
 * # BrisaccessCtrl
 * Controller of the repoApp
 */
angular.module('repoApp')
  .controller('BrisaccessCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
