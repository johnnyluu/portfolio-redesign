'use strict';

/**
 * @ngdoc function
 * @name repoApp.controller:TuitionCtrl
 * @description
 * # TuitionCtrl
 * Controller of the repoApp
 */
angular.module('repoApp')
  .controller('TuitionCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
