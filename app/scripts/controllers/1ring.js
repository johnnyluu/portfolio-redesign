'use strict';

/**
 * @ngdoc function
 * @name repoApp.controller:1ringCtrl
 * @description
 * # 1ringCtrl
 * Controller of the repoApp
 */
angular.module('repoApp')
  .controller('1ringCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
