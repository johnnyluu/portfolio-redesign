'use strict';

/**
 * @ngdoc function
 * @name repoApp.controller:ContactCtrl
 * @description
 * # ContactCtrl
 * Controller of the repoApp
 */
angular.module('repoApp')
  .controller('ContactCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
