'use strict';

/**
 * @ngdoc function
 * @name repoApp.controller:LacasarosaCtrl
 * @description
 * # LacasarosaCtrl
 * Controller of the repoApp
 */
angular.module('repoApp')
  .controller('LacasarosaCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
