'use strict';

/**
 * @ngdoc function
 * @name repoApp.controller:WaterbottleCtrl
 * @description
 * # WaterbottleCtrl
 * Controller of the repoApp
 */
angular.module('repoApp')
  .controller('WaterbottleCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
