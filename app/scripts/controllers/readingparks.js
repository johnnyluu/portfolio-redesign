'use strict';

/**
 * @ngdoc function
 * @name repoApp.controller:ReadingparksCtrl
 * @description
 * # ReadingparksCtrl
 * Controller of the repoApp
 */
angular.module('repoApp')
  .controller('ReadingparksCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
